import connectToDatabase from '../src/lib/mongodb/connection'\nimport { User } from '../src/lib/mongodb/models/User'\nimport { Story } from '../src/lib/mongodb/models/Story'\n\n// Script to initialize the database with indexes and sample data\nasync function initializeDatabase() {\n  try {\n    console.log('Connecting to database...')\n    await connectToDatabase()\n    \n    console.log('Creating indexes...')\n    \n    // Create indexes for User model\n    await User.createIndexes()\n    console.log('User indexes created')\n    \n    // Create indexes for Story model\n    await Story.createIndexes()\n    console.log('Story indexes created')\n    \n    console.log('Database initialization complete!')\n    \n  } catch (error) {\n    console.error('Error initializing database:', error)\n  } finally {\n    process.exit(0)\n  }\n}\n\n// Run the script\ninitializeDatabase()\n